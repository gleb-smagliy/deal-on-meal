import moduleId from '../constants/module-id';

export default {
  USER_JOIN: `${moduleId}/USER_JOIN`,
  USER_LEFT: `${moduleId}/USER_LEFT`,
  DISPATCH_CLIENT_ACTION: `${moduleId}/DISPATCH_CLIENT_ACTION`,
  EMIT_CLIENT_ACTION: `${moduleId}/EMIT_CLIENT_ACTION`
};
